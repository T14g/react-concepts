import styled from "styled-components";

const HeaderStyles = styled.header`
    ul {
        list-style: none;

        li {
            button{
                background: none;
                border: none;
            }
        }
    }
`;

export default HeaderStyles;